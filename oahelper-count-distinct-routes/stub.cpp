#include <bits/stdc++.h>
using namespace std;

long long solve(const string& plan, long long n, long long start_zone, long long end_zone) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string plan;
    long long n, s, e;
    if (!(cin >> plan >> n >> s >> e)) return 0;
    cout << solve(plan, n, s, e) << "\n";
    return 0;
}
