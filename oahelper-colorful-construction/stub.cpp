#include <bits/stdc++.h>
using namespace std;

long long solve(int r, int b) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int r, b;
    if (!(cin >> r >> b)) return 0;
    cout << solve(r, b) << "\n";
    return 0;
}
