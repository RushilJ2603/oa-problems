#include <bits/stdc++.h>
using namespace std;

int solve(const string& series) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string series;
    if (!(cin >> series)) return 0;
    cout << solve(series) << "\n";
    return 0;
}
