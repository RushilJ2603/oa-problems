#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long g[4][4];
    for (int i = 0; i < 4; ++i)
        for (int j = 0; j < 4; ++j) cin >> g[i][j];
    int N;
    cin >> N;
    // For each of N presses (U/D/L/R), slide + merge, and print the cumulative score.
    // your code here
    return 0;
}
