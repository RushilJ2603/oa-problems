#include <bits/stdc++.h>
using namespace std;

// Return the decimal string equal to (number + 1), where `number` is given as a digit string.
string addOne(string number) {
    // WRITE YOUR CODE HERE
    return number;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    if (!(cin >> s)) return 0;
    cout << addOne(s) << "\n";
    return 0;
}
