#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long x, y, z;
    cin >> x >> y >> z;
    // Count n in [y, z] such that n*x shares no digit with n.
    // your code here
    return 0;
}
