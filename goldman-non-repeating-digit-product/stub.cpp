#include <bits/stdc++.h>
using namespace std;

// Count the integers n in [y, z] such that n and n*x share no digit.
long long countValid(long long x, long long y, long long z) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long x, y, z;
    if (cin >> x >> y >> z) cout << countValid(x, y, z) << "\n";
    return 0;
}
