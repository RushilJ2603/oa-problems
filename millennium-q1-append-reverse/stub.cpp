#include <iostream>
#include <string>

using namespace std;

string solve(string s) {
    // WRITE YOUR CODE HERE
    return "";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    if (cin >> s) {
        cout << solve(s) << "\n";
    }
    return 0;
}
