#include <bits/stdc++.h>
using namespace std;

char solve(const string& s) {
    // WRITE YOUR CODE HERE
    return 'a';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    if (!(cin >> s)) return 0;
    cout << solve(s) << "\n";
    return 0;
}
