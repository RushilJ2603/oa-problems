#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll minOpsToPalindrome(ll n) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    if (!(cin >> t)) return 0;
    while (t--) {
        ll n;
        cin >> n;
        cout << minOpsToPalindrome(n) << "\n";
    }
    return 0;
}
