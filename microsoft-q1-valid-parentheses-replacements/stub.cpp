#include <bits/stdc++.h>
using namespace std;

// Return 1 if s can be made a valid parenthesis string using at most k
// replacements of a single ')' with "()", else 0.
int solve(const string &s, long long k) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s; long long k;
    cin >> s >> k;
    cout << solve(s, k) << "\n";
}
