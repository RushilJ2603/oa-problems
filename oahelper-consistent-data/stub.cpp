#include <iostream>

using namespace std;

int solve(int n) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    if (cin >> n) {
        cout << solve(n) << "\n";
    }
    return 0;
}
