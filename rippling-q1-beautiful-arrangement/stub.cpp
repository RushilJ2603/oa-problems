#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    // Count permutations of 1..n where for every 1-based i: p[i]%i==0 or i%p[i]==0.
    // your code here
    return 0;
}
