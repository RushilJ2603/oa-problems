#include <bits/stdc++.h>
using namespace std;

// Count permutations p of 1..n where for every 1-based i: p[i]%i==0 or i%p[i]==0.
long long countArrangements(int n) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    if (cin >> n) cout << countArrangements(n) << "\n";
    return 0;
}
