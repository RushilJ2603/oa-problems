import sys

def solve():
    tok = iter(sys.stdin.read().split())
    # Read inputs with next(tok) (see the Problem tab for the exact input format), e.g.
    #   n = int(next(tok))
    #   arr = [int(next(tok)) for _ in range(n)]
    # then compute the answer and print it.
    # WRITE YOUR CODE HERE

def main():
    solve()

if __name__ == "__main__":
    main()
