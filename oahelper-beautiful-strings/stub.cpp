#include <bits/stdc++.h>
using namespace std;

long long solve(long long N, int K) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long N;
    int K;
    if (!(cin >> N >> K)) return 0;
    cout << solve(N, K) << "\n";
    return 0;
}
