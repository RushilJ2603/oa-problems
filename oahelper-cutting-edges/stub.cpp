#include <bits/stdc++.h>
using namespace std;

int solve(const string& s) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    if (!(cin >> s)) return 0;
    cout << solve(s) << "\n";
    return 0;
}
