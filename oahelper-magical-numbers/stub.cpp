#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int minMagicalTerms(ll n) {
    // WRITE YOUR CODE HERE
    return -1;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    if (!(cin >> t)) return 0;
    while (t--) {
        ll n;
        cin >> n;
        cout << minMagicalTerms(n) << "\n";
    }
    return 0;
}
