#include <bits/stdc++.h>
using namespace std;

// Minimum days to turn price n into exactly m using, each day, -1 or *2.
int minDays(int n, int m) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m;
    if (!(cin >> n >> m)) return 0;
    cout << minDays(n, m) << "\n";
    return 0;
}
