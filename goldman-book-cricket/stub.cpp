#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long n;
    cin >> n;
    // Count ordered sequences of {2,4,6} summing to n.
    // your code here
    return 0;
}
