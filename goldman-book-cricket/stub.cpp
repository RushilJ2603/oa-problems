#include <bits/stdc++.h>
using namespace std;

// Count the ordered sequences drawn from {2, 4, 6} that sum to exactly n.
long long countSequences(long long n) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long n;
    if (cin >> n) cout << countSequences(n) << "\n";
    return 0;
}
