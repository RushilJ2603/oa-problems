#include <bits/stdc++.h>
using namespace std;

int solve(int n, string& s, int k) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    string s;
    if (!(cin >> n)) return 0;
    cin >> s >> k;
    cout << solve(n, s, k) << "\n";
    return 0;
}
