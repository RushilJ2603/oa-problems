#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    cin >> s;
    // Return the length of the longest palindromic subsequence of s.
    // your code here
    return 0;
}
