#include <bits/stdc++.h>
using namespace std;

long long solve(const string& s, int k) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    int k;
    if (!(cin >> s >> k)) return 0;
    cout << solve(s, k) << "\n";
    return 0;
}
