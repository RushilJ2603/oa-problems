#include <bits/stdc++.h>
using namespace std;

string solve(int n, int k, string s) {
    // WRITE YOUR CODE HERE
    return "-1";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    if (!(cin >> n >> k)) return 0;
    string s;
    cin >> s;
    cout << solve(n, k, s) << "\n";
    return 0;
}
