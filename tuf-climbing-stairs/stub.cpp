#include <bits/stdc++.h>
using namespace std;

// Return the number of distinct ways to climb to step n taking 1 or 2 steps at a time.
long long climbStairs(int n) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    if (cin >> n) cout << climbStairs(n) << "\n";
    return 0;
}
