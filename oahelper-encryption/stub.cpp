#include <bits/stdc++.h>
using namespace std;

long long solve(const string& S) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string S;
    if (!(cin >> S)) return 0;
    cout << solve(S) << "\n";
    return 0;
}
