#include <iostream>
#include <string>

using namespace std;

// WRITE YOUR CODE HERE
long long solve(string s, long long D, long long Seg) {
    return 0;
}

int main() {
    string s;
    long long D, Seg;
    if (cin >> s >> D >> Seg) {
        cout << solve(s, D, Seg) << "\n";
    }
    return 0;
}
