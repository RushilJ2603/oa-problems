#include <bits/stdc++.h>
using namespace std;

long long solve(const string& word) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string word;
    if (!(cin >> word)) return 0;
    cout << solve(word) << "\n";
    return 0;
}
