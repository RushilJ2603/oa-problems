#include <bits/stdc++.h>
using namespace std;

long long solve(long long m, long long k) {
    // WRITE YOUR CODE HERE
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long m, k;
    if (!(cin >> m >> k)) return 0;
    cout << solve(m, k) << "\n";
    return 0;
}
